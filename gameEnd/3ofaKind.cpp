#include<iostream>
#include<vector>
using namespace std;

int threeKind(vector<int>, int);
int main(){

}

int threeKind(vector<int> handNum, int handSize){
    bool threeKind = false;
    for (int card = 0; card < handSize - 1; card++){
        for (int nextCard = card + 1; nextCard < handSize; nextCard++){
            
        }
}
